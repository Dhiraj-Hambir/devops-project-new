pipeline {
    agent { 
        node {
            label 'maven-agent' 
        }
    }

    stages {
        stage('Check Master Environment') {
            steps {
                echo 'Running on Jenkins Master...'
                // 'okok' folder create karel
                sh 'mkdir -p okok' 
                
               
            }
        }
    }
}
